point à vérifier :

- Mettre status 201 à toutes les données qui sont créer
- ne pas avoir d'erreur 500 sauf dans le cas d'un catch
- Avec GET si l'objet n'existe pas, doit renvoyer un 404
- Filtre optionnel à faire (pas de concatenation)