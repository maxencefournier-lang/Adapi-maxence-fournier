info:
  name: postobjet
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/objets/89
  body:
    type: json
    data: |-
      {
        "libelle": "montre",
        "poids_kg": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
